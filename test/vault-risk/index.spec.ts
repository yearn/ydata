import { expect } from 'chai';

describe('borrito', function () {
  it('goes to school', function () {
    expect(true).to.equal(true);
  });
  it('does homework', function () {
    expect(true).to.equal(false);
  });
  it('codes for yearn', function () {
    expect(true).to.equal(false);
  });
});
